apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: deepak-daemon
spec:
  selector:
    matchLabels:
      tier: deepak-daemon-pod
  template:
    metadata:
      labels:
        tier: deepak-daemon-pod
    spec:
      containers:
      - image: nginx
        name: daemonset-deepak-second
